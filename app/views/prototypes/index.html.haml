= render partial: 'prototypes/semi_header', locals: { type: @type }

.container.proto-list
  .row
    = render partial: 'prototypes/prototype', collection: @prototypes

.text-center
  = paginate @prototypes
